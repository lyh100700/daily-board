# 안드로이드 앱 (캘비캘린더)

웹앱을 안드로이드 앱으로 감싼 것입니다. 화면과 기능은 웹앱과 같고,
앱 껍데기만 따로 만들어 폰에 설치했습니다.

## 지금 상태

| 항목 | 값 |
|---|---|
| 앱 이름 | 캘비캘린더 |
| 패키지 이름 | `io.github.lyh100700.twa` |
| 방식 | TWA (웹앱을 감싼 안드로이드 앱) |
| 최소 안드로이드 | 6.0 |
| 첫 배포 | 2026-08-13 (v1.0.0) |

## ⚠️ 서명 키는 저장소에 없습니다

`~/Documents/캘비-릴리스/` 폴더에 따로 두었습니다.

**키를 잃어버리면 기존 앱을 업데이트할 수 없습니다.**
설치된 앱을 지우고 새로 깔아야 하며, 그때 일정 데이터도 함께 사라집니다.
외장하드나 클라우드에 **반드시 백업**해 두세요.

공개 저장소에 올리면 누구나 이 앱의 업데이트를 위조할 수 있으므로
`.gitignore`로 막아 두었습니다.

---

## 내용만 고칠 때 — 앱을 다시 만들 필요 없음

**대부분의 수정은 여기에 해당합니다.**

일정 기능, 디자인, 상식·단어 콘텐츠, 공휴일 같은 것은 전부 웹에 있습니다.
평소처럼 고쳐서 올리면 앱에도 그대로 반영됩니다.

```bash
# sw.js 의 VERSION 숫자를 올린 뒤
git add -A
git commit -m "무엇을 고쳤는지"
git push
```

1~2분 뒤 배포되고, 앱을 껐다 켜면 자동으로 갱신됩니다.

---

## 앱 자체를 다시 만들어야 할 때

이런 경우에만 APK를 새로 만듭니다.

- 앱 이름이나 아이콘을 바꿀 때
- 앱 주소(start_url)가 바뀔 때
- 안드로이드 정책 변경으로 다시 빌드해야 할 때

### 1. PWABuilder 에서 껍데기 받기

https://www.pwabuilder.com

1. 주소 입력 → **Start**
   ```
   https://lyh100700.github.io/daily-board/
   ```
2. **Package For Stores** → **Android** → **Generate Package**
3. **`Other Android` 탭**을 고릅니다 ⭐
   - `Google Play` 탭은 서명 없는 파일이 나와 폰에 설치되지 않습니다
4. **Package ID 를 기존과 똑같이** 맞춥니다
   ```
   io.github.lyh100700.twa
   ```
   다르면 기존 앱을 덮어쓰지 못하고 별개의 앱으로 깔립니다.
5. **Download Package**

### 2. 서명하기

`~/Documents/캘비-릴리스/서명하기.command` 를 **더블클릭**하세요.

받은 `-unsigned.apk` 를 고르면 서명된 파일이 같은 폴더에 만들어집니다.
필요한 도구(자바, 안드로이드 서명 도구)는 없으면 알아서 받습니다.

> 직접 명령어로 하려면 `키-정보.txt` 에 별칭과 비밀번호가 적혀 있습니다.
> `zipalign` 으로 정렬한 뒤 `apksigner` 로 v1·v2·v3 서명을 모두 넣어야
> 최신 안드로이드에 설치됩니다.

### 3. 폰에 설치

구글 드라이브에 올렸다가 폰에서 받는 방법이 가장 확실합니다.
카카오톡은 APK 파일을 제대로 못 보내는 경우가 있습니다.

설치할 때 뜨는 경고는 정상입니다.

- *출처를 알 수 없는 앱* → 허용
- *Play 프로텍트가 검사* → 무시하고 설치

구글에 등록되지 않은 앱이면 누구나 겪는 절차입니다.

---

## 주소 표시줄 없애기 (아직 안 함)

지금 앱은 실행할 때 위쪽에 주소가 잠깐 보입니다.
"이 앱이 그 사이트의 주인이 맞다"는 증명이 없어서 그렇습니다.

없애려면 아래 파일을 **사이트 최상위 주소**에 올려야 합니다.

```
https://lyh100700.github.io/.well-known/assetlinks.json
```

`/daily-board/` 안쪽이 아니라 최상위여야 하므로,
**`lyh100700.github.io` 라는 이름의 저장소를 따로 만들어야** 합니다.
(GitHub 에서 계정 이름과 같은 저장소는 최상위 주소가 됩니다)

올릴 파일 내용은 이 저장소의 `assetlinks.json` 에 준비해 두었습니다.
앱을 새 키로 다시 서명하면 지문이 바뀌므로 이 파일도 함께 갱신해야 합니다.

---

## 처음 만들 때 겪은 문제들

같은 함정에 다시 빠지지 않도록 남깁니다.

| 증상 | 원인 |
|---|---|
| 브라우저에 설치 버튼이 없음 | 삼성 인터넷은 신호를 보내지 않음. 크롬을 쓰거나 `현재 페이지 추가 → 홈 화면` |
| APK 설치가 거부됨 | PWABuilder 의 `Google Play` 탭은 **서명 없는** 파일을 줌. `Other Android` 를 쓸 것 |
| 서명했는데도 설치 안 됨 | v1 서명만으로는 부족. **v2·v3 까지** 넣어야 최신 안드로이드가 받아들임 |
| 파일이 폰에서 안 열림 | 카카오톡 전송 문제. 구글 드라이브를 거칠 것 |
